import React from "react"

import { Grid, Typography } from "@material-ui/core";

const Header = () => {
    return (
        <Grid
        container
        justify="center"
        >
            <Typography variant="h1">Wondermart</Typography>
        </Grid>
    )
}

export default Header